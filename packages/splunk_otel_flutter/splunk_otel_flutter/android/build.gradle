group = "com.splunk.rum.flutter"
version = "1.0-SNAPSHOT"

buildscript {
    ext.kotlin_version = "2.1.0"
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath("com.android.tools.build:gradle:8.9.1")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version")
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

apply plugin: "com.android.library"
apply plugin: "kotlin-android"

android {
    namespace = "com.splunk.rum.flutter"

    compileSdk = 36

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_11
        targetCompatibility = JavaVersion.VERSION_11
        coreLibraryDesugaringEnabled(true)
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_11
    }

    sourceSets {
        main.java.srcDirs += "src/main/kotlin"
        test.java.srcDirs += "src/test/kotlin"
    }

    defaultConfig {
        minSdk = 24
    }

    dependencies {
        // Core agent API (SplunkRum singleton, AgentConfiguration, etc.)
        implementation("com.splunk:rum-integration-agent-api:2.1.4")

        // Integration modules used by this plugin
        implementation("com.splunk:rum-integration-anr:2.1.4")
        implementation("com.splunk:rum-integration-applicationlifecycle:2.1.4")
        implementation("com.splunk:rum-integration-crash:2.1.4")
        implementation("com.splunk:rum-integration-customtracking:2.1.4")
        implementation("com.splunk:rum-integration-httpurlconnection-auto:2.1.4")
        implementation("com.splunk:rum-integration-interactions:2.1.4")
        implementation("com.splunk:rum-integration-navigation:2.1.4")
        implementation("com.splunk:rum-integration-networkmonitor:2.1.4")
        implementation("com.splunk:rum-integration-okhttp3-auto:2.1.4")
        implementation("com.splunk:rum-integration-okhttp3-manual:2.1.4")
        implementation("com.splunk:rum-integration-slowrendering:2.1.4")
        implementation("com.splunk:rum-integration-startup:2.1.4")

        compileOnly("com.splunk:rum-integration-sessionreplay:2.1.4")

        coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.1.3")

        testImplementation("org.jetbrains.kotlin:kotlin-test")
        testImplementation("org.mockito:mockito-core:5.0.0")
    }

    testOptions {
        unitTests.all {
            useJUnitPlatform()

            testLogging {
               events "passed", "skipped", "failed", "standardOut", "standardError"
               outputs.upToDateWhen {false}
               showStandardStreams = true
            }
        }
    }
}
