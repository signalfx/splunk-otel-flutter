name: splunk_otel_flutter_root_example_app
description: "Splunk Flutter Agent root example app "
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0
  flutter: '>=3.32.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  splunk_otel_flutter: ^0.0.1
  splunk_otel_flutter_session_replay: ^0.0.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true