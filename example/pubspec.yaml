name: splunk_otel_flutter_example
description: "Demonstrates how to use the splunk_otel_flutter plugin."

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  splunk_otel_flutter:
    path: ../

  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
